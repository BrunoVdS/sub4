The activity parity keeps its own read

D7 slice B3, and it is §12.101 one slice later. ShadowParity.run took the
app side of slices 1 and 2 from ActivityStore.shared.activities. 382
hydrates that store from the database, so both sides of ActivityParity and
VolumeParity would have been the same rows — agreeing perfectly, proving
nothing, and saying so nowhere. No test could have caught it, because two
sides agreeing is exactly what a pass looks like.

The order is the patch. If the flip had landed first there would have been
a build in which Compare printed "no differences" about the database and
itself, and the number that catches that is the one this patch adds.

The answer was already in the tree twice. 343 gave PlanRoundTrip an
independent side by decoding the bundle; 356 gave the authored read-back
its own read of notes.json, commutes.json and moves.json. activities.json
is still written and still complete, so ActivitySource.read() reads it —
through ActivityStore(directory:), the seam 378 added, and not through a
second decoder. 364's rule: one decoder, one settle, two roots.

Three states, not two. A container the app cannot reach and a device that
has never synced both produce an empty array, and §12.15 is that those are
opposite facts. directoryFound and the StoreLoad keep them apart and the
line says which. When the file cannot be read the comparison falls back to
the store and appSideWasReadCleanly goes false — and isHealthy now
requires it, because zero differences over an app side that could not be
read is not a pass. After 382 that fallback IS the database.

The enumeration is what made this patch exist, and it found more than the
flip. AppStores.gather feeds the verifier and the importer from the same
list: activities, activity identities and volume by discipline all take
their expectation from it and stop being evidence at 382 — three entries
for HydratedStores, not the one §12.123 predicted. Seven more expectations
are filtered by storeIDs, which is derived from the same list. And slices
3, 5 and 8 take their app side through LoadStore, Matcher and TabSummary,
which read the store by design; handing those a shadow list would mean
comparing something the athlete's screens do not show. That loss is
recorded in §12.125.4 rather than left to be discovered in a paste.

A defect found while writing it, unreachable and fixed anyway.
ActivityStore.load() calls recordRejections, which writes the shared
strava.rejections key. A store rooted elsewhere starts with receipts EMPTY
— loadRejections runs in private init() only — so one self-contradictory
row in the file it read would have written a blob holding that row alone,
replacing the three receipts this device has kept since 278. Those
describe recordings that are in no file and that the cursor moved past
years ago: §12.8.1, and nothing could re-fetch them. It cannot fire today
because a refused activity is never written to activities.json. It is
fixed because of the shape rather than the odds — a write nobody can
trigger until the day somebody can — and 381 is the patch that gave that
file its second reader.

Nothing is flipped. hydratedFamilies still names six and HydratedStores
still holds five, and a guard fails the patch otherwise. The negative this
patch is — ShadowParity must not ask the store — is held by the apply
script rather than by a test, for 356's reason: the store and the file
hold the same activities today, which is precisely why this patch changes
no number.

Verified by applying to a byte-exact copy of the tree and running
check-invariants there. Not verified by a compiler.

ADR-0003 §12.125.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_014SitAH7xM7FcCVknnkwWYM
