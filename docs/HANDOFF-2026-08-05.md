# Sub4 — next session handoff

Written 4 August 2026, 22:15, at patch 245. Everything below is verified on the
device unless it says otherwise.

---

## 1. Where the rebuild actually is

Against the remediation plan's own D0–D8 ladder:

| Stage | State |
|---|---|
| D0 Freeze & capture | **Not done** — the duplicate `plan.json` question was never resolved |
| D1 Foundation | Done |
| D2 Canonical schema | Done — 50 tables, 9 migrations, on the device |
| **D3 Legacy importer** | **Largely skipped — this is tomorrow's work** |
| D4 User/plan cutover | ~70% |
| D5 Activity cutover | ~75% |
| D6 Shadow parity | Not started |
| D7 Activate | Not started |
| D8 Stabilise & retire | Not started |

**Note on numbering.** Earlier sessions used "3.3.1 / 3.3.2 / 3.3.3", which are
NOT the plan's numbers. The plan's 3.3 is the migration engine (D3); making the
database authoritative is the plan's **3.7 / D7**.

### What is in the database, on the phone

50 tables, 139,482 rows, 23.8 MB, integrity ok, 0 orphaned rows, foreign keys
on, prepared at launch.

661 activities + aliases + source records · 6 gear + 473 references · 4 notes ·
575 weather · athlete profile + 5 zones + 14 resting months · plan across 3
versions (37 weeks, 260 sessions, 634 blocks each) · 415 traces with 124,323
samples · 420 details with 5,428 splits, 1,483 laps, 537 best efforts.

**Refused: 1**, permanently and by decision — the August 2025 Romania recording.
ADR-0003 §12.12.5 has the evidence. A second entry there is news.

### The fourteen empty tables are the work not done

`match_decision`, `correction`, `sync_state`, `work_queue`, `content_revision`,
`lifecycle_event`, `lifecycle_line`, `review`, `review_evidence`,
`review_evidence_source`, `proposal`, `proposal_change`, `proposal_watch`.

That is exactly plan items 3.4.4, 3.5.4 and 3.6.

---

## 2. The four deviations to keep in mind

1. **No protected snapshot.** The migration contract item 3 requires copying
   every legacy input into a dated protected snapshot with path, byte count and
   SHA-256 *before decoding*. Never done. Two reinstalls this week destroyed
   `notes.json`, `weather.json`, the UserDefaults gates and HealthKit
   authorisation. This is the most important missing piece in the project.
2. **No `migration_run` ledger.** Nothing can be `verified` or `activated`
   because no such state exists.
3. **The import reads the live stores, not the files.** A deliberate choice,
   argued in `Sub4Import`'s header — a cutover's target is what the app *shows*.
   The cost: absent, empty, corrupt and truncated input cannot be told apart,
   because no legacy byte is ever read. Both halves can coexist; only one
   exists.
4. **Import order is inverted** relative to the contract, which wants the plan
   and irreplaceable data first. We did activities first and the plan sixth.

---

## 3. Tomorrow: D3, in four patches

Do these in order. Each is device-verified before the next starts.

### Patch 246 — the manifest and the protected snapshot

- Build the manifest from `DataInventory`, which is already the single source of
  truth for what the app writes and is already covered by tests ("The declared
  paths are exactly the ones the stores write").
- For every declared path: absolute path, exists, byte count, mtime, SHA-256.
  A missing file is *recorded as missing*, never skipped.
- Copy — never move — into `snapshots/<yyyy-MM-dd-HHmmss>/`, with
  `manifest.json` beside it. File protection on the folder, same as the database
  directory.
- Surface it: last snapshot, file count, total bytes, and a "Snapshot now"
  button in Database health.

**Acceptance:** every path `DataInventory` declares appears in the manifest;
hashing the same file twice gives the same digest; a deliberately deleted file
shows as missing rather than vanishing from the list; the snapshot survives a
relaunch and is readable.

### Patch 247 — the `migration_run` ledger

- New migration adding `migration_run`: id, startedUTC, finishedUTC, state,
  snapshotID, appVersion, note. State CHECK in
  `pending / running / verified / activated / failed` — frozen vocabulary, held
  to a Swift enum by test, like every other vocabulary in this schema.
- The import opens a row, moves it through the states, and closes it.
- Database health shows the current state and the last run.

**Acceptance:** an import that throws leaves `failed`, not `running`; two
imports produce two rows; the state vocabulary and the enum agree.

### Patch 248 — file-level decoders and quarantine

- Read the legacy files directly, alongside (not instead of) the store-based
  import.
- Correct decoder per store: notes and proposals use ISO-8601 dates, several
  others use the default numeric `Date` encoding. Getting this wrong silently
  produces 1970.
- Classify each input: missing, empty, corrupt, truncated, key-mismatch,
  unsupported-schema. Validate outer dictionary keys against the embedded
  `sessionUid` / `activityId` and quarantine mismatches rather than picking one.
- `quarantine` table plus a recovery screen listing what was held back and why.

**Acceptance:** a truncated fixture is reported as truncated, not empty; a
key-mismatch is quarantined rather than resolved; an absent file on a fresh
install is not an error.

### Patch 249 — the semantic verifier

- Compare the database against the stores: counts, id sets, content
  fingerprints, and a handful of *representative domain outputs* — CTL on a
  chosen day, one week's volume, one activity's splits, one weather reading.
- `migration_run` reaches `verified` only when the verifier passes, and the
  screen shows what was compared rather than a single green tick.

**Acceptance:** deliberately deleting one row from the database makes the
verifier fail and name the table; a clean run reports every comparison it made.

---

## 4. After D3

5. Finish **D4** — match overrides, corrections, rejection receipts; saves
   become `async throws`; editors dismiss only after commit; Retry / Export
   Copy / Cancel on failure.
6. Finish **D5** — sync cursors, rejects, retry queues, backfill versions and
   content revisions out of UserDefaults into typed rows.
7. **D6** shadow parity — read both, compare in diagnostics, zero unexplained
   divergence.
8. **D7** activate, in the plan's slice order: plan/notes/proposals → profile/
   constants/zones/gear → matches → activities → details/streams/weather → sync
   state. Includes the export/restore/delete drill. **This is where
   `Sub4Launch.migrationFailureBlocksTheApp` flips to `true`.**
9. **D8** stabilise one release window, then remove the JSON writers.

Phase 4A (Apple Health becomes canonical) cannot start before D7's exit gate.

**Also outstanding from D0:** resolve the two differing `plan.json` files and
designate one as the only versioned seed. The plan puts this before everything
else and it has never been done.

---

## 5. Dated items

- **24 August 2026** — first real monthly review. The proposals importer has
  never seen anything but a fixture. ADR-0003 §12.8.2 has the checklist.
- **1 September 2026** — GitHub Actions allowance resets. CI has been unverified
  since the triggers changed to weekly + tags + manual; run it manually once.
- **Ongoing** — the detail and trace backfill is still running, so those counts
  climb on every import. Not drift.

---

## 6. How this project works (for a fresh session)

- Patches are **zips**, unzipped by the athlete into
  `~/Documents/Developer/sub4/Sub4/Sub4/`; tests into `Sub4CoreTests/`.
  Markdown files have not been reaching Downloads reliably — **send documents as
  zips too**.
- `AppVersion.swift` ships in **every** patch zip, with the number bumped. A
  stale number is how you learn the patch did not land.
- Never use Xcode's "Add Files" — the project uses file-system-synchronized
  groups. A **new** file needs ⌘Q and reopen; an overwritten one does not.
- No git, and no writing into the repo through the device bridge. Reading is
  fine. The athlete runs every git command.
- Always give exact steps and say where on screen a thing is — read the view
  first rather than guessing at a screen's contents.

Test command:

```bash
cd ~/Documents/Developer/sub4/Sub4
xcodebuild test -project Sub4.xcodeproj -scheme Sub4 \
  -destination 'id=8528BBD2-5C8C-4D51-94A2-87F34FA9B4BA' \
  2>&1 | tee ~/Downloads/sub4-tests.log | grep -E "Test run with|✘|warning:|error:|TEST"
```

**380 tests in 39 suites** at patch 245. `xcodebuild` will happily run a cached
test bundle and report success for code it never compiled — if the count does
not move when it should, `xcodebuild clean` first.

---

## 7. What this project has learned, and keeps re-learning

- **Real data beats tests.** Every serious defect this week was found by reading
  a number on the phone: the FTP endpoint that never carried the field, the plan
  whose totals needed 66 km/h on a bike, twelve details lost to a zero heart
  rate, a migration body edited after it had run while eight tests stayed green.
- **Write the ADR mapping against the Swift type before writing the importer.**
  Six for six on finding missing schema.
- **A green suite cannot see a schema that diverged from its migrations.** Tests
  build from source; a device holds what its history built. `sqlite_master` is
  the only witness.
- **`nonisolated` is a claim that code can run anywhere**, and it obliges
  everything it touches. Not a tidiness marker. Six instances this week.
- **A control that reports work it did not do is worse than one that fails.**
  Five of those were fixed this week; assume there are more.
